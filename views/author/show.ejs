<h1 class="page-header"><%= author.name  %></h1>

<div class="btn-row">
    <a class="btn btn-primary" href="/authors/<%= author.id %>/edit">Edit</a>
    <%- include("../partials/deleteform.ejs", {url: `/authors/${author.id}`}) %>
</div>

<% if(booksByAuthor.length > 0){ %>
    <h2 class="page-header">Books By this Author</h2>

    <%- include('../partials/bookGrid', {books: booksByAuthor}) %>
    
<% } %>